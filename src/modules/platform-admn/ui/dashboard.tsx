// This component has a dual nature or role.
// It is basically a composer component which composes  the varous dashboards
// defined and published by the modules.
// At the mean time, this component acts as the UI as well.
// That is to show the composed dashboard as the dashboard of the ERP system.
// Therefore the root route is linked with this component.
// When the root route is requested, this component is loaded in the UI.

export default function Dashboard() {
  return 'Dashboard';
}
